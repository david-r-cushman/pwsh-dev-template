interface:
  display_name: "Downstream Guidance Sync"
  short_description: "Sync template AI guidance into downstream repos"
  default_prompt: "Use $downstream-guidance-sync to sync a downstream repository with the current pwsh-dev-template AI guidance."
